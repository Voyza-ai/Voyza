# macOS
.DS_Store

# Editor
.vscode/
.idea/

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Dependencies (each workspace has its own node_modules)
**/node_modules/

# Env files — real values are per-workspace
# (each workspace has its own .gitignore for specifics)

# Claude Code session-local — contains worktree state and MCP credentials
.claude/worktrees/
.claude/launch.json
.mcp.json
